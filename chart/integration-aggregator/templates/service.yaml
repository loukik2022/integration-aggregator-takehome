apiVersion: v1
kind: Service
metadata:
  name: {{ include "integration-aggregator.fullname" . }}
  labels:
    {{- include "integration-aggregator.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "integration-aggregator.selectorLabels" . | nindent 4 }}
